================================================================================
        LAPORAN UML DIAGRAMS - MediTrack Platform
        Tanggal: 25 Maret 2026
        Status: Diagram UML Komprehensif
================================================================================

KUMPULAN UML DIAGRAMS MEDITRACK
===============================================================================

1. UML CLASS DIAGRAM - COMPLETE SYSTEM
----------------------------

┌────────────────────────────────────────────────────────────────────────────┐
│                         USER & IDENTITY CONTEXT                           │
└────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────┐
│          User (Abstract)     │
├──────────────────────────────┤
│ - id: int                    │
│ - name: string               │
│ - email: string              │
│ - password: string           │
│ - phone: string              │
│ - address: string            │
│ - role: enum                 │
│ - status: enum               │
│ - created_at: datetime       │
│ - updated_at: datetime       │
├──────────────────────────────┤
│ + authenticate(pwd): bool    │
│ + hasPermission(perm): bool  │
│ + hasRole(role): bool        │
│ + getRoles(): Collection     │
│ + getPermissions(): Collection
│ + updateProfile(data): void  │
│ + changePassword(new): void  │
│ + logout(): void             │
│ + getAuditLog(days): []      │
└──────────┬───────────────────┘
           │
           │ extends/implements
           │
    ┌──────┼──────┬──────┐
    │      │      │      │
    ▼      ▼      ▼      ▼
   ┌──┐  ┌──┐  ┌──┐  ┌──┐
   │PA│  │PH│  │DO│  │AD│
   │TI│  │AR│  │CT│  │MI│
   │EN│  │MA│  │OR│  │N │
   │TS│  │CI│  │  │  │  │
   │  │  │ST│  │  │  │  │
   └────────────────────────┘
   (Concrete implementations)


┌───────────────────────────────┐
│         Role                  │
├───────────────────────────────┤
│ - id: int                     │
│ - name: string                │
│ - description: string         │
│ - permissions: Collection<Perm
│ - is_active: boolean          │
├───────────────────────────────┤
│ + addPermission(perm): void   │
│ + removePermission(perm): void│
│ + hasPermission(perm): bool   │
│ + getPermissions(): []        │
└───────────────────────────────┘
           ▲
           │ many-to-many
           │
┌──────────┴──────────────────┐
│      Permission             │
├─────────────────────────────┤
│ - id: int                   │
│ - name: string              │
│ - resource: string          │
│ - action: string            │
│ (e.g., "users", "create")   │
├─────────────────────────────┤
│ + grant(to_role): void      │
│ + revoke(from_role): void   │
│ + checkAccess(user): bool   │
└─────────────────────────────┘


┌─────────────────────────────┐
│      AuditLog               │
├─────────────────────────────┤
│ - id: int                   │
│ - user_id: int (FK)         │
│ - action: string            │
│ - resource: string          │
│ - old_values: json          │
│ - new_values: json          │
│ - ip_address: string        │
│ - user_agent: string        │
│ - timestamp: datetime       │
├─────────────────────────────┤
│ + logAction(action): void   │
│ + getHistory(user_id): []   │
│ + export(format): file      │
└─────────────────────────────┘


┌────────────────────────────────────────────────────────────────────────────┐
│                      APPOINTMENT SCHEDULING CONTEXT                        │
└────────────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────┐
│      Appointment               │
├────────────────────────────────┤
│ - id: int (PK)                 │
│ - patient_id: int (FK)         │
│ - doctor_id: int (FK)          │
│ - appointment_date: datetime   │
│ - status: enum                 │
│   [scheduled, confirmed, ...] │
│ - type: string [regular, ...]  │
│ - location: string             │
│ - duration: int (minutes)      │
│ - description: text            │
│ - notes: text                  │
│ - cancellation_reason: text    │
│ - created_at: datetime         │
│ - updated_at: datetime         │
├────────────────────────────────┤
│ + book(): bool                 │
│ + confirm(): bool              │
│ + reschedule(date): bool       │
│ + cancel(reason): bool         │
│ + getStatus(): enum            │
│ + sendReminder(): void         │
│ + sendNotification(type): void │
│ + hasConflict(): bool          │
│ + isValid(): bool              │
│ RELATIONS:                     │
│ + patient: User                │
│ + doctor: User                 │
│ + medicalRecords: []           │
│ + payment: Payment             │
│ + prescriptions: []            │
│ + notifications: []            │
└────────────────────────────────┘
       ▲
       │ uses
    ┌──┴──────────────────────────┐
    │                             │
┌───┴─────────────────────┐  ┌───┴────────────────────┐
│  AppointmentService     │  │ AppointmentValidator   │
├─────────────────────────┤  ├────────────────────────┤
│ - repo: Repository      │  │ + validateDateTime()   │
│ - notifService: Notif   │  │ + checkConflict()      │
├─────────────────────────┤  │ + validateDuration()   │
│ + bookAppointment()     │  │ + checkMinNotice()     │
│ + updateAppointment()   │  │ + validateLocation()   │
│ + cancelAppointment()   │  │ + validateStatus()     │
│ + getAvailability()     │  └────────────────────────┘
│ + getDoctorSchedule()   │
│ + sendReminders()       │
│ + autoReschedule()      │
└─────────────────────────┘

┌──────────────────────────────┐
│   AppointmentNotification    │
├──────────────────────────────┤
│ - id: int                    │
│ - appointment_id: int (FK)   │
│ - type: enum [sms, email]    │
│ - recipient: string          │
│ - message: text              │
│ - sent_at: datetime          │
│ - status: enum               │
├──────────────────────────────┤
│ + send(): bool               │
│ + markAsSent(): void         │
│ + retry(): bool              │
│ + getSendLog(): []           │
└──────────────────────────────┘


┌────────────────────────────────────────────────────────────────────────────┐
│                    CLINICAL & MEDICAL RECORDS CONTEXT                      │
└────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────┐
│    MedicalRecord             │
├──────────────────────────────┤
│ - id: int (PK)               │
│ - patient_id: int (FK)       │
│ - doctor_id: int (FK)        │
│ - appointment_id: int (FK)   │
│ - diagnosis: text            │
│ - treatment: text            │
│ - lab_results: json          │
│ - notes: text                │
│ - encrypted: boolean         │
│ - created_at: datetime       │
│ - updated_at: datetime       │
├──────────────────────────────┤
│ + create(): void             │
│ + getHistory(limit): []      │
│ + export(format): file       │
│ + encrypt(): void            │
│ + decrypt(): object          │
│ + addLabResult(lab): void    │
│ + updateNotes(text): void    │
│ + getAccessLog(): []         │
│ + canAccess(user): bool      │
│ RELATIONS:                   │
│ + patient: User              │
│ + doctor: User               │
│ + appointment: Appointment   │
│ + labResults: []             │
│ + prescriptions: []          │
└──────────────────────────────┘

┌──────────────────────────────┐
│    Prescription              │
├──────────────────────────────┤
│ - id: int (PK)               │
│ - patient_id: int (FK)       │
│ - doctor_id: int (FK)        │
│ - appointment_id: int (FK)   │
│ - medication_id: int (FK)    │
│ - dosage: string             │
│ - frequency: string          │
│ - duration_days: int         │
│ - refills_left: int          │
│ - status: enum               │
│ - issued_date: date          │
│ - expiry_date: date          │
├──────────────────────────────┤
│ + create(): void             │
│ + submit(): void             │
│ + approve(): bool            │
│ + refill(): bool             │
│ + sendToPharmacy(): bool     │
│ + getOrders(): []            │
│ RELATIONS:                   │
│ + drug: Drug                 │
│ + patient: User              │
│ + doctor: User               │
│ + prescriptionOrders: []     │
└──────────────────────────────┘

┌──────────────────────────────┐
│     LabResult                │
├──────────────────────────────┤
│ - id: int                    │
│ - patient_id: int (FK)       │
│ - test_type: string          │
│ - result_value: mixed        │
│ - reference_range: string    │
│ - unit: string               │
│ - status: enum [normal, ...]│
│ - lab_date: datetime         │
│ - reported_at: datetime      │
├──────────────────────────────┤
│ + upload(file): void         │
│ + process(): void            │
│ + notify(): void             │
│ + getInterpretation(): text  │
│ + compareWithPrevious(): obj │
└──────────────────────────────┘

┌──────────────────────────────┐
│ MedicalRecordService         │
├──────────────────────────────┤
│ - recordRepo: Repository     │
│ - encryptionService: Encrypt │
│ - accessCtrl: AccessControl  │
├──────────────────────────────┤
│ + createRecord(data): Record │
│ + getHistory(patient): []    │
│ + exportRecord(id): pdf      │
│ + shareRecord(user): void    │
│ + checkAccess(rec, user)     │
│ + auditAccess(rec_id): []    │
└──────────────────────────────┘


┌────────────────────────────────────────────────────────────────────────────┐
│                    PHARMACY & DRUG STOCK CONTEXT                           │
└────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────┐
│      Pharmacy                │
├──────────────────────────────┤
│ - id: int (PK)               │
│ - name: string               │
│ - address: string            │
│ - phone: string              │
│ - manager_id: int (FK)       │
│ - license_number: string     │
│ - status: enum               │
│ - created_at: datetime       │
├──────────────────────────────┤
│ + updateStock(drug, qty): b  │
│ + processOrder(order): bool  │
│ + getInventory(): obj        │
│ + checkAvailability(drug): b │
│ + generateReorderList(): []  │
│ + getMetrics(): obj          │
│ RELATIONS:                   │
│ + drugStocks: []             │
│ + orders: []                 │
│ + transactions: []           │
└──────────────────────────────┘

┌──────────────────────────────┐
│     DrugStock                │
├──────────────────────────────┤
│ - id: int (PK)               │
│ - pharmacy_id: int (FK)      │
│ - drug_id: int (FK)          │
│ - quantity: int              │
│ - reorder_level: int         │
│ - reorder_quantity: int      │
│ - expiry_date: date          │
│ - batch_number: string       │
│ - received_at: datetime      │
├──────────────────────────────┤
│ + decreaseStock(qty): bool   │
│ + increaseStock(qty): bool   │
│ + isLowStock(): bool         │
│ + checkExpiry(): bool        │
│ + getMovementHistory(): []   │
│ + calculateExpecto(): int    │
│ RELATIONS:                   │
│ + drug: Drug                 │
│ + pharmacy: Pharmacy         │
│ + transactions: []           │
└──────────────────────────────┘

┌──────────────────────────────┐
│        Drug                  │
├──────────────────────────────┤
│ - id: int (PK)               │
│ - name: string               │
│ - generic_name: string       │
│ - category: string           │
│ - manufacturer: string       │
│ - batch_price: decimal       │
│ - retail_price: decimal      │
│ - description: text          │
│ - side_effects: text         │
│ - contraindications: text    │
├──────────────────────────────┤
│ + getAvailability(): []      │
│ + getCheapestSource(): pharm │
│ + getUsageStats(): obj       │
│ + checkInteractions(drug): []
│ RELATIONS:                   │
│ + stocks: []                 │
│ + prescriptions: []          │
└──────────────────────────────┘

┌──────────────────────────────┐
│ PrescriptionOrder            │
├──────────────────────────────┤
│ - id: int (PK)               │
│ - prescription_id: int (FK)  │
│ - pharmacy_id: int (FK)      │
│ - status: enum               │
│ [new, accepted, dispensed..]│
│ - ordered_at: datetime       │
│ - accepted_at: datetime      │
│ - fulfilled_at: datetime     │
├──────────────────────────────┤
│ + submit(): void             │
│ + accept(): bool             │
│ + dispense(): bool           │
│ + updateStatus(status): void │
│ + notifyPatient(): void      │
│ + getTrackingInfo(): obj     │
│ RELATIONS:                   │
│ + prescription: Prescription │
│ + pharmacy: Pharmacy         │
└──────────────────────────────┘

┌──────────────────────────────┐
│ InventoryTransaction         │
├──────────────────────────────┤
│ - id: int                    │
│ - drug_id: int (FK)          │
│ - pharmacy_id: int (FK)      │
│ - type: enum [in, out, adj]  │
│ - quantity: int              │
│ - reason: string             │
│ - reference_id: string       │
│ - created_by: int (FK)       │
│ - timestamp: datetime        │
├──────────────────────────────┤
│ + log(): void                │
│ + reverseTransaction(): bool │
│ + generateReport(): []       │
└──────────────────────────────┘


┌────────────────────────────────────────────────────────────────────────────┐
│                    PAYMENT & FINANCIAL CONTEXT                             │
└────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────┐
│      Payment                 │
├──────────────────────────────┤
│ - id: int (PK)               │
│ - patient_id: int (FK)       │
│ - appointment_id: int (FK)   │
│ - amount: decimal            │
│ - currency: string           │
│ - payment_method: enum       │
│ - status: enum               │
│ - transaction_id: string     │
│ - gateway_response: json     │
│ - paid_at: datetime          │
│ - created_at: datetime       │
├──────────────────────────────┤
│ + process(): bool            │
│ + capture(): bool            │
│ + refund(amount): bool       │
│ + getReceipt(): pdf          │
│ + verifyTransaction(): bool  │
│ + getRefundStatus(): str     │
│ RELATIONS:                   │
│ + invoice: Invoice           │
│ + insuranceClaim: Insurance  │
│ + transactions: []           │
└──────────────────────────────┘

┌──────────────────────────────┐
│       Invoice                │
├──────────────────────────────┤
│ - id: int (PK)               │
│ - patient_id: int (FK)       │
│ - appointment_id: int (FK)   │
│ - invoice_number: string     │
│ - subtotal: decimal          │
│ - tax_amount: decimal        │
│ - total_amount: decimal      │
│ - issued_date: date          │
│ - due_date: date             │
│ - payment_date: date         │
│ - status: enum               │
│ - notes: text                │
├──────────────────────────────┤
│ + generate(): void           │
│ + send(): void               │
│ + markPaid(): void           │
│ + createReminder(): void     │
│ + export(format): file       │
│ + getPaymentTerms(): str     │
│ RELATIONS:                   │
│ + payment: Payment           │
│ + items: []                  │
└──────────────────────────────┘

┌──────────────────────────────┐
│   InsuranceClaim             │
├──────────────────────────────┤
│ - id: int (PK)               │
│ - patient_id: int (FK)       │
│ - appointment_id: int (FK)   │
│ - claim_amount: decimal      │
│ - claim_date: date           │
│ - status: enum [pending, ..] │
│ - approved_amount: decimal   │
│ - response_date: date        │
│ - claim_reference: string    │
│ - denial_reason: text        │
├──────────────────────────────┤
│ + submit(): void             │
│ + track(): string            │
│ + updateStatus(): void       │
│ + autoApprove(): void        │
│ + deny(reason): void         │
│ + appealdecision(): void     │
│ RELATIONS:                   │
│ + insuranceProvider: Insurer │
│ + payment: Payment           │
└──────────────────────────────┘

┌──────────────────────────────┐
│   PaymentService             │
├──────────────────────────────┤
│ - paymentRepo: Repository    │
│ - gatewayAdapter: Adapter    │
│ - claimService: ClaimService │
├──────────────────────────────┤
│ + processPayment(data): pay  │
│ + captureFunds(id): bool     │
│ + refundPayment(id): bool    │
│ + submitClaim(appt): claim   │
│ + generateInvoice(appt): inv │
│ + trackPayment(id): obj      │
│ + handleWebhook(event): void │
│ + scheduleReminder(inv): void│
└──────────────────────────────┘


┌────────────────────────────────────────────────────────────────────────────┐
│                      ANALYTICS & REPORTING CONTEXT                         │
└────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────┐
│ PatientOutcomeMetric         │
├──────────────────────────────┤
│ - id: int                    │
│ - patient_id: int (FK)       │
│ - metric_type: string        │
│ - value: decimal             │
│ - unit: string               │
│ - measurement_date: datetime │
│ - notes: text                │
├──────────────────────────────┤
│ + calculate(): decimal       │
│ + aggregate(period): obj     │
│ + trend(months): chart       │
│ + compare(patient2): obj     │
└──────────────────────────────┘

┌──────────────────────────────┐
│ DoctorPerformanceMetric      │
├──────────────────────────────┤
│ - id: int                    │
│ - doctor_id: int (FK)        │
│ - metric_type: string        │
│ │ [completion_rate, ...]     │
│ - value: decimal             │
│ - benchmark: decimal         │
│ - percentage_rank: decimal   │
│ - measurement_period: date   │
├──────────────────────────────┤
│ + calculate(): decimal       │
│ + benchmark(peers): obj      │
│ + trend(months): chart       │
│ + improve(date_range): tips  │
│ + compareWithAverage(): pct  │
└──────────────────────────────┘

┌──────────────────────────────┐
│ DrugUsageAnalytic            │
├──────────────────────────────┤
│ - id: int                    │
│ - drug_id: int (FK)          │
│ - usage_count: int           │
│ - total_quantity: decimal    │
│ - period_start: date         │
│ - period_end: date           │
│ - trend: enum [up, flat, dn] │
│ - forecast_qty: int          │
├──────────────────────────────┤
│ + track(): void              │
│ + predictTrend(): trend      │
│ + forecastDemand(days): qty  │
│ + compareWithHistory(): obj   │
│ + identifyOutliers(): []     │
└──────────────────────────────┘

┌──────────────────────────────┐
│       Report                 │
├──────────────────────────────┤
│ - id: int                    │
│ - report_type: string        │
│ - parameters: json           │
│ - generated_at: datetime     │
│ - generated_by: int (FK)     │
│ - file_path: string          │
│ - status: enum               │
│ - refresh_interval: string   │
├──────────────────────────────┤
│ + generate(): void           │
│ + export(format): file       │
│ + schedule(cron): void       │
│ + email(recipients): void    │
│ + getLatest(): obj           │
│ + subscribe(user): void      │
└──────────────────────────────┘

┌──────────────────────────────┐
│ AnalyticsService             │
├──────────────────────────────┤
│ - metricRepo: Repository     │
│ - eventProcessor: Processor  │ 
│ - reportGen: Generator       │
├──────────────────────────────┤
│ + calculateMetrics(): void   │
│ + aggregateData(period): obj │
│ + generateReport(type): rpt  │
│ + predictTrends(months): obj │
│ + identify Bottleneck(data)  │
│ + sendNotifications(): void  │
└──────────────────────────────┘

---

2. UML SEQUENCE DIAGRAM - APPOINTMENT BOOKING FLOW
----------------------------

Actor: Patient | System:                     | Payment
(Web Browser)  | (MediTrack Backend)         | Gateway
      │             │                        │
      │─Request──→  │                        │
      │ Book        │                        │
      │ Appointment │                        │
      │             │                        │
      │             │─Validate Auth─→ (JWT) │
      │             │                        │
      │             │─Check─→ Doctor        │
      │             │ Availability           │
      │             │                        │
      │             │─Query─→ Appointment   │
      │             │ Table                 │
      │             │ (Check conflicts)     │
      │             │←─No Conflicts─        │
      │             │                        │
      │             │─Insert─→ Database     │
      │             │ New Booking           │
      │             │←─Success─             │
      │             │                        │
      │             │─Publish Event:─→      │
      │             │ AppointmentCreated    │
      │             │                        │
      │             │─Queue Job─→           │
      │             │ SendNotification      │
      │             │                        │
      │             │─Invalidate Cache──→   │
      │             │ (Doctor Availability) │
      │             │                        │
      │←Response─   │                        │
      │ Success     │                        │
      │             │                        │
      │           (Background Job Execution)│
      │             │                        │
      │             │─Send Notification─→   │
      │             │ To Patient (SMS/Email)│
      │             │                        │
      │             │─Send Notification─→   │
      │             │ To Doctor             │
      │             │                        │
      │             │─Log Analytics─→ Event│
      │             │ Listener              │

---

3. UML USE CASE DIAGRAM - SYSTEM ACTORS & INTERACTIONS
----------------------------

                        ┌─────────────────────┐
                        │   MEDITRACK         │
                        │   SYSTEM            │
                        │                     │
                        │  ┌───────────────┐  │
                        │  │ Browse Doctor │  │
                        │  │ Availability  │  │
                        │  └───────────────┘  │
                        │         ▲           │
                        │         │           │
   ┌──────────┐ ┌───────┴────────┴────────┐  │
   │          │ │                        │  │
   │         ┌──Book               Resume│  │
   │         │►Appointment           ────┤  │
   │       ┌─┴──────────────────────────┴┐  │
   │       │                             │  │
   │      ││  ┌─ Pay for ─────────────◄──┤  │
   │      ││  │ Appointment           │  │  │
   │      ││  │   (async)              │  │  │
   │      ││  └─────────────┬──────────┘  │  │
   │      │└─────────────────┤────────────┘  │
   │      │                  │               │
  PATIENT│  ┌────────────────┴───────────┐  │
   │      └──│View Medical │             │  │
   │      │  │  Records    │             │  │
   │      │  └─────────────┘             │  │
   │      │                              │  │
   │      │  ┌──────────────────────┐    │  │
   │      └─►│ Get Prescription     │    │  │
   │         │ Orders (via Pharmacy)│    │  │
   │         └──────────────────────┘    │  │
   │                                     │  │
   └──────────────────────────────────────┘  │
          ▲                                   │
          │                                   │
          │                                   │
   ┌──────┴──────────────────────────────┐   │
   │                                     │   │
   │  ┌─ Create Appointment ─────────┐   │   │
   │  │ • Check conflicts            │   │   │
   │  │ • Assign doctor              │   │   │
   │  │ • Send notifications          │   │   │
   │  └──────────────────────────────┘   │   │
   │                                     │   │
   │  ┌─ Write Prescription ──────────┐  │   │
   │  │ • Select medications          │  │   │
   │  │ • Set dosage                  │  │   │
   │  │ • Send to pharmacy             │  │   │
   │  └──────────────────────────────┘  │   │
   │                                     │   │
   │  ┌─ Create Medical Record ──────┐   │   │
   │  │ • Document diagnosis         │   │   │
   │  │ • Store treatment            │   │   │
   │  │ • Attach lab results         │   │   │
   │  └──────────────────────────────┘   │   │
  DOCTOR│                                 │   │
   │  ┌─ View Patient History ────────┐  │   │
   │  │ • Previous appointments       │  │   │
   │  │ • Medical records             │  │   │
   │  │ • Treatments                  │  │   │
   │  └──────────────────────────────┘  │   │
   │                                     │   │
   └─────────────────────────────────────┘   │
          ▲                                   │
          │                                   │
          │                                   │
   ┌──────┴──────────────────────────────┐   │
   │                                     │   │
   │  ┌─ Process Orders ──────────────┐  │   │
   │  │ • Check stock                 │  │   │
   │  │ • Prepare medicines            │  │   │
   │  │ • Dispense prescriptions       │  │   │
   │  └──────────────────────────────┘  │   │
   │                                     │   │
PHARMACIST  ┌─ Manage Inventory ────────┐  │   │
   │  │ • Update stock                │  │   │
   │  │ • Set reorder levels          │  │   │
   │  │ • Track expiry dates          │  │   │
   │  └──────────────────────────────┘  │   │
   │                                     │   │
   └─────────────────────────────────────┘   │
          ▲                                   │
          │                                   │
          │                                   │
   ┌──────┴────────────────────────────┐    │
   │                                   │    │
   │  ┌─ Manage Users ────────────────┐│   │
   │  │ • Create/Update users        ││   │
   │  │ • Assign roles               ││   │
   │  │ • Set permissions             ││   │
   │  └───────────────────────────────┘   │
   │                                   │   │
   │  ┌─ Generate Reports ─────────────┐│   │
   │  │ • Patient outcomes             ││   │
   │  │ • Doctor performance           ││   │
   │  │ • Drug usage trends            ││   │
   │  └───────────────────────────────┘│   │
   │                                   │   │
   │  ┌─ Monitor System Health ───────┐│   │
   │  │ • Check performance            ││   │
   │  │ • View logs                    ││   │
   │  │ • Manage alerts                ││   │
   │  └───────────────────────────────┘   │
   │                                    │   │
      ADMIN                                 │
   └───────────────────────────────────┘   │
                                            │
                        └─────────────────────┘

---

4. UML COMPONENT DIAGRAM - SYSTEM ARCHITECTURE
----------------------------

┌─────────────────────────────────────────────────────────────────────┐
│                     MEDITRACK PLATFORM                             │
│                                                                    │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  PRESENTATION LAYER                                        │  │
│  │                                                            │  │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐     │  │
│  │  │ Web UI       │ │ Mobile App   │ │ Admin Panel  │     │  │
│  │  │ (React/Vue)  │ │ (React Native)│ │ (Dashboard)  │     │  │
│  │  └──────┬───────┘ └──────┬───────┘ └──────┬───────┘     │  │
│  │         │                │                │             │  │
│  │  ┌──────┴────────────────┴────────────────┴──────┐      │  │
│  │  │ HTTP/HTTPS Transport Layer                   │      │  │
│  │  └──────┬─────────────────────────────────────────┘     │  │
│  │         │                                               │  │
│  │  ┌──────┴──────────────────────────────────────────┐   │  │
│  │  │ API Gateway / Load Balancer                    │   │  │
│  │  │ • Rate Limiting                                │   │  │
│  │  │ • Request Validation                           │   │  │
│  │  │ • SSL/TLS Termination                          │   │  │
│  │  └──────┬───────────────────────────────────────────┘   │  │
│  └─────────┼────────────────────────────────────────────────┘  │
│            │                                                    │
│  ┌─────────┴──────────────────────────────────────────────┐    │
│  │  APPLICATION LAYER                                     │    │
│  │                                                        │    │
│  │  ┌──────────────────────────────────────────────┐    │    │
│  │  │ Controllers (HTTP Endpoints)                 │    │    │
│  │  │ • UserController                             │    │    │
│  │  │ • AppointmentController                      │    │    │
│  │  │ • MedicalRecordController                    │    │    │
│  │  │ • PaymentController                          │    │    │
│  │  │ • PharmacyController                         │    │    │
│  │  │ • AnalyticsController                        │    │    │
│  │  └──────┬───────────────────────────────────────┘    │    │
│  │         │                                            │    │
│  │  ┌──────┴──────────────────────────────────────────┐ │    │
│  │  │ Middleware Layer                                │ │    │
│  │  │ • Authentication (JWT Validation)               │ │    │
│  │  │ • Authorization (RBAC)                          │ │    │
│  │  │ • Request Logging                               │ │    │
│  │  │ • Error Handling                                │ │    │
│  │  └──────┬──────────────────────────────────────────┘ │    │
│  │         │                                            │    │
│  │  ┌──────┴──────────────────────────────────────────┐ │    │
│  │  │ Services Layer (Business Logic)                 │ │    │
│  │  │ • UserService                                   │ │    │
│  │  │ • AppointmentService                            │ │    │
│  │  │ • MedicalRecordService                          │ │    │
│  │  │ • PharmacyService                               │ │    │
│  │  │ • PaymentService                                │ │    │
│  │  │ • AnalyticsService                              │ │    │
│  │  └──────┬──────────────────────────────────────────┘ │    │
│  │         │                                            │    │
│  │  ┌──────┴──────────────────────────────────────────┐ │    │
│  │  │ Repository Layer (Data Access Abstraction)      │ │    │
│  │  │ • UserRepository                                │ │    │
│  │  │ • AppointmentRepository                         │ │    │
│  │  │ • MedicalRecordRepository                       │ │    │
│  │  │ • PaymentRepository                             │ │    │
│  │  │ • PharmacyRepository                            │ │    │
│  │  │ • AnalyticsRepository                           │ │    │
│  │  └──────┬──────────────────────────────────────────┘ │    │
│  │         │                                            │    │
│  └─────────┼────────────────────────────────────────────┘    │
│            │                                                  │
│  ┌─────────┴──────────────────────────────────────────────┐  │
│  │  SUPPORTING SERVICES                                   │  │
│  │                                                        │  │
│  │  ┌────────────────┐ ┌────────────────┐                │  │
│  │  │ Cache Service  │ │ Queue Service  │                │  │
│  │  │ (Redis)        │ │ (Job Queue)    │                │  │
│  │  └────────┬───────┘ └────────┬───────┘                │  │
│  │           │                  │                        │  │
│  │  ┌────────┴──────┐ ┌─────────┴──────┐                │  │
│  │  │ Notification  │ │ Event          │                │  │
│  │  │ Service       │ │ Listener       │                │  │
│  │  │ (SMS/Email)   │ │ (Analytics)    │                │  │
│  │  └───────────────┘ └────────────────┘                │  │
│  │                                                        │  │
│  └────────────────────────────────────────────────────────┘  │
│            │                                                  │
│  ┌─────────┴──────────────────────────────────────────────┐  │
│  │  DATA ACCESS LAYER                                     │  │
│  │                                                        │  │
│  │  ┌────────────────────┐  ┌──────────────────────┐    │  │
│  │  │ Primary Database   │  │ Cache Layer          │    │  │
│  │  │ (MySQL/PostgreSQL) │  │ (Redis In-Memory)    │    │  │
│  │  │                    │  │                      │    │  │
│  │  │ • Users            │  │ • Session Data       │    │  │
│  │  │ • Appointments     │  │ • Query Cache        │    │  │
│  │  │ • Medical Records  │  │ • Permission Matrix  │    │  │
│  │  │ • Prescriptions    │  │ • Doctor Schedules   │    │  │
│  │  │ • Payments         │  │ • Report Templates   │    │  │
│  │  │ • Pharmacy Data    │  │                      │    │  │
│  │  └────────────────────┘  └──────────────────────┘    │  │
│  │                                                        │  │
│  └────────────────────────────────────────────────────────┘  │
│            │                                                  │
│  ┌─────────┴──────────────────────────────────────────────┐  │
│  │  EXTERNAL INTEGRATIONS                                 │  │
│  │                                                        │  │
│  │  ┌────────────────┐  ┌────────────────┐               │  │
│  │  │ Payment        │  │ Insurance      │               │  │
│  │  │ Gateway API    │  │ Provider API   │               │  │
│  │  │ (Stripe)       │  │                │               │  │
│  │  └────────────────┘  └────────────────┘               │  │
│  │                                                        │  │
│  │  ┌────────────────┐  ┌────────────────┐               │  │
│  │  │ Notification   │  │ Pharmacy       │               │  │
│  │  │ Service        │  │ Integration    │               │  │
│  │  │ (Twilio)       │  │ APIs           │               │  │
│  │  └────────────────┘  └────────────────┘               │  │
│  │                                                        │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                               │
└─────────────────────────────────────────────────────────────────┘

---

5. UML STATE DIAGRAM - APPOINTMENT LIFECYCLE
----------------------------

                              │
                              ▼
                      ┌──────────────────┐
                      │   NEW REQUEST    │
                      │ (User initiates) │
                      └────────┬─────────┘
                               │
                      ┌────────▼─────────┐
                      │   VALIDATING     │
                      │ • Check doctor   │
                      │ • Check conflict │
                      │ • Validate time  │
                      └────┬──────────┬──┘
                           │          │
                    Success│          │ Fail
                           │          │
                      ┌────▼─┐   ┌───▼─────────┐
                      │PENDING│   │ REJECTED    │
                      │       │   │ (Show error)│
                      └────┬──┘   └─────────────┘
                           │
                    ┌──────▼────────┐
                    │ Patient Pays  │
                    │ (Async Job)   │
                    └──┬─────────┬──┘
                       │         │
                Payment│         │ Payment
                Success│         │ Failed
                       │         │
                  ┌────▼────┐ ┌─▼──────────┐
                  │CONFIRMED│ │ ABANDONED  │
                  │ • Send  │ │ + Cleanup  │
                  │ notif  │ │            │
                  └────┬────┘ └────────────┘
                       │
            ┌──────────┬┴───────────────────┐
            │                             │
       ┌────▼─────┐            ┌──────────▼────────┐
       │COMPLETED │            │RESCHEDULED/CANCELLED
       │ (Done)   │            │ + reason needed   │
       └──────────┘            └───────────────────┘

This UML Class Diagram, Sequence Diagram, Use Case Diagram, Component 
Diagram, and State Diagram together provide a complete view of the MediTrack
system architecture and interactions.

===============================================================================
                    END OF LAPORAN UML DIAGRAMS
===============================================================================
